import { login } from "./utils.js";

document.addEventListener("DOMContentLoaded", () => {
  //salvare in una variabile il form di login
  //salvare in una variabile il csrf token
  //aggiungere all'evento submit del form una funziona asincrona che dovra:
  //prevenire il submit di default
  //chiamare la funzione login (gia importata) e passargli i relativi parametri
});
